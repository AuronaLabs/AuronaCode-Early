# Aurona VSCode Demo Extension

这是一个标准的 VSCode 插件包（`.vsix`），用于演示和验证 **Aurona Code 的底层 VSCode 兼容转译引擎**。

## 特性
- 标准 VSCode API 编写（`vscode.commands`、`vscode.window`、`vscode.ExtensionContext`）；
- 由 Aurona 底层 WASM 沙箱零开销原生转译运行；
- 完美适配 Aurona 现代化毛玻璃 Toast 通知与状态栏。
